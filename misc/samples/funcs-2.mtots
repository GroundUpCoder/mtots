
fun rec(n) {
    if (n <= 1) {
        return 1;
    }
    return n + rec(n - 1);
}

print(rec(50));

var total = 0;
for (var i = 1; i <= 50; i = i + 1) {
    total = total + i;
}

print(total); # should be same as 'rec(50)'
